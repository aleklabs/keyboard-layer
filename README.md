# keyboard-layer
Alek Labs Keyboard Layer is a non-modal AutoHotkey command layer that keeps standard QWERTY typing intact while bringing navigation, selection, deletion, clipboard operations, undo, redo, and Enter closer to the home position.
